settings:
  default_pipeline_ctrl_msg_channel_size: 100
  default_node_ctrl_msg_channel_size: 100
  default_pdata_channel_size: 100

nodes:
  receiver:
    kind: receiver
    plugin_urn: "urn:otel:otlp:receiver"
    out_ports:
      out_port:
        destinations:
          - debug
        dispatch_strategy: round_robin
    config:
      listening_addr: "127.0.0.1:4319"
      # NOTE: Replace the following certificate and key file paths with the actual locations of your TLS certificate and key.
      # Alternatively, use cert_pem and key_pem for in-memory PEM configuration.
      tls:
        cert_file: "test_certs/server.crt"  # Relative to working directory, or use absolute path like "/etc/otap/certs/server.crt"
        key_file: "test_certs/server.key"  # Relative to working directory, or use absolute path
        client_ca_file: "test_certs/ca.crt"  # Relative to working directory, or use absolute path. Enables mTLS - remove to disable client auth
        reload_interval: "30s"  # Hot-reload certificates
  debug:
    kind: processor
    plugin_urn: "urn:otel:debug:processor"
    out_ports:
      out_port:
        destinations:
          - noop
        dispatch_strategy: round_robin
    config:
      verbosity: detailed
  noop:
    kind: exporter
    plugin_urn: "urn:otel:noop:exporter"
    config:
