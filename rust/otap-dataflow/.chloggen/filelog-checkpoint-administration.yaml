# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: enhancement

# The broad Rust component affected by this change.
component: pipeline

# A brief description of the user-visible change.
note: Add offline dfctl commands to inspect, validate, back up, and administer quarantined filelog checkpoint records.

# Tracking issue for the Phase 1 filelog receiver.
issues: [2844]

# Additional operator-facing context.
subtext: |
  Mutations require exclusive ownership and audited reasons. Per-file actions
  require exact file, epoch, and replacement-source evidence. Corrupt or
  missing authority can be validated and backed up, but cannot be replaced.
