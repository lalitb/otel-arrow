# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: bug_fix

# The broad Rust component affected by this change.
component: pipeline

# A brief description of the user-visible change.
note: Align filelog reconciliation and EOF reprobe cadence with the Phase 1 contract.

# Tracking issue for the Phase 1 filelog receiver.
issues: [2844]

# Additional operator-facing context.
subtext: |
  Replace discovery.poll_interval with discovery.reconcile_interval, add
  discovery.reconcile_jitter_percent, and configure reader.eof_reprobe_interval.
