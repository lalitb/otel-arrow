# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: bug_fix

# The broad Rust component affected by this change.
component: pipeline

# A brief description of the user-visible change.
note: Prevent duplicate or rolled-back filelog checkpoint transactions after uncertain WAL writes or sync failures.

# Tracking issue for the Phase 1 filelog receiver.
issues: [2844]

# Additional operator-facing context.
subtext: |
  The receiver now validates and repairs the exact final WAL append before a
  bounded retry, preserving complete transactions and truncating only torn ones.
