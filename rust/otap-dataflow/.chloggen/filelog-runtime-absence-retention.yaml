# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: bug_fix

# The broad Rust component affected by this change.
component: pipeline

# A brief description of the user-visible change.
note: Base filelog checkpoint retention on continuous runtime-proven absence.

# Tracking issue for the Phase 1 filelog receiver.
issues: [2844]

# Additional operator-facing context.
subtext: |
  Restart, incomplete discovery, or source reappearance resets the monotonic
  absence interval. Quiet namespaces wake and revalidate at the retention deadline.
