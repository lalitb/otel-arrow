# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: bug_fix

# The broad Rust component affected by this change.
component: pipeline

# A brief description of the user-visible change.
note: Bound filelog source and discovery retries under transient filesystem and descriptor pressure.

# Tracking issue for the Phase 1 filelog receiver.
issues: [2844]

# Additional operator-facing context.
subtext: |
  Startup checks the receiver-owned descriptor budget against the Unix soft
  limit. Temporary source failures preserve progress without quarantine and
  retry per file, traversal root, or receiver-global descriptor condition.
